const fish = {
    name: 'King Hilsha Ilisha',
    address: 'chandapur',
    color: 'Silver',
    price: 4000
}


const { price } = fish;
console.log(price);

const { age, name, height } = { name: "Akash", age: 25, height: 5.5 };
console.log(name, age, height);